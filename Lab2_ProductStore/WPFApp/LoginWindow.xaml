<Window x:Class="WPFApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFApp"
        mc:Ignorable="d"
        Title="Login - Product Management" Height="360" Width="480"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F7"
        ResizeMode="NoResize">
    <Grid>
        <Border CornerRadius="8" Margin="20" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Padding="30,20,30,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="2" Opacity="0.1"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Product Store System" FontSize="22" FontWeight="SemiBold" Foreground="#333333" HorizontalAlignment="Center"/>
                    <TextBlock Text="Please log in to continue" FontSize="12" Foreground="#666666" Margin="0,5,0,0" HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Input Fields -->
                <StackPanel Grid.Row="1" VerticalAlignment="Center">
                    <TextBlock Text="Username" FontSize="12" FontWeight="Medium" Foreground="#444444" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtUser" Height="32" FontSize="14" VerticalContentAlignment="Center" Padding="5,0,5,0" 
                             BorderBrush="#CCCCCC" BorderThickness="1" Margin="0,0,0,15"/>

                    <TextBlock Text="Password" FontSize="12" FontWeight="Medium" Foreground="#444444" Margin="0,0,0,5"/>
                    <PasswordBox x:Name="txtPass" Height="32" FontSize="14" VerticalContentAlignment="Center" Padding="5,0,5,0" 
                                 BorderBrush="#CCCCCC" BorderThickness="1" Margin="0,0,0,5"/>
                </StackPanel>

                <!-- Action Buttons -->
                <Grid Grid.Row="2" Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="btnLogin" Grid.Column="0" Content="LOG IN" Height="36" Background="#0078D4" Foreground="White" 
                            FontWeight="SemiBold" BorderThickness="0" Cursor="Hand" Click="btnLogin_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#0078D4"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#106EBE"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button x:Name="btnCancel" Grid.Column="2" Content="CANCEL" Height="36" Background="#E0E0E0" Foreground="#333333" 
                            FontWeight="SemiBold" BorderThickness="0" Cursor="Hand" Click="btnCancel_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#E0E0E0"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#D0D0D0"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
